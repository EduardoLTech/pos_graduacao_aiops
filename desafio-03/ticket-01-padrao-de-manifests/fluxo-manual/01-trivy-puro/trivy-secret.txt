$ trivy fs -q --scanners secret manifests/

Report Summary

┌────────┬──────┬─────────┐
│ Target │ Type │ Secrets │
├────────┼──────┼─────────┤
│   -    │  -   │    -    │
└────────┴──────┴─────────┘
Legend:
- '-': Not scanned
- '0': Clean (no security findings detected)


# Controle positivo (mesmo diretório + controle.yaml com um token no formato ghp_...):
# trivy fs -q --scanners secret --format json .
#   controle.yaml -> [('github-pat', 'GitHub Personal Access Token')]
#   nyx-barrado.yaml -> nenhum achado
# Conclusão: o scanner lê YAML e reconhece padrão de provedor, mas não
# credencial genérica em URL (postgres://usuario:senha@host). A regra 3.3 fica com o script.
