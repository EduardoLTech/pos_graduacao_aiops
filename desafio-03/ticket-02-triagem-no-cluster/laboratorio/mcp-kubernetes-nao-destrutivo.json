{
  "mcpServers": {
    "kubernetes": {
      "command": "npx",
      "args": ["-y", "mcp-server-kubernetes@4.1.7"],
      "env": {
        "KUBECONFIG": "${TRIAGEM_KUBECONFIG}",
        "ALLOW_ONLY_NON_DESTRUCTIVE_TOOLS": "true",
        "MASK_SECRETS": "true"
      }
    }
  }
}
